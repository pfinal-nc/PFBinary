; PFSerialize — example php.ini snippet
;
; After: bash php/install.sh
; Or point extension= at the built .so path.

extension=pfserialize.so

; Optional: absolute path if not in extension_dir
; extension=/usr/local/lib/php/pecl/20250925/pfserialize.so
